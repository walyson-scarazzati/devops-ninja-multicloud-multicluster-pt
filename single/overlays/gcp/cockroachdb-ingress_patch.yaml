spec:
  rules:
    - host: cockroachdb.multicloud-devops-ninja.pt
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cockroachdb
                port:
                  number: 8080
