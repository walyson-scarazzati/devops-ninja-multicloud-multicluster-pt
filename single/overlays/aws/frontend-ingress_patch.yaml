spec:
  rules:
    - host: app.devops-ninja.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mc-frontend
                port:
                  number: 80
